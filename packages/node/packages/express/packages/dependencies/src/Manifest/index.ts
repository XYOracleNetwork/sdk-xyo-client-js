import defaultNode from './node.json'
import { imageThumbnailNode } from './PublicChildren'

/**
 * The public children of the node
 */
export const publicChildren = [imageThumbnailNode]

export {
  /**
   * The default node
   */
  defaultNode,
}
