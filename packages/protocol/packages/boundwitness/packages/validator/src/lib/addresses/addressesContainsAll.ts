import type { Address } from '@xylabs/hex'
import type { BoundWitnessFields } from '@xyo-network/boundwitness-model'
import { BoundWitness } from '@xyo-network/boundwitness-model'

export const addressesContainsAll = (boundwitness: Partial<BoundWitnessFields>, addresses: Address[]): boolean => {
  // TODO
  return false
}
