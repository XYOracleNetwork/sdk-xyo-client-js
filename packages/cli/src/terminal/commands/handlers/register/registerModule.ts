import { NodeModule } from '@xyo-network/node'

import { printError, printTitle } from '../../../../lib'

export const registerModule = (_node: NodeModule) => {
  printTitle('Register Module')
  printError('TODO')
  return Promise.resolve()
}
