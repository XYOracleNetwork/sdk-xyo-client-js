import { DirectNodeModule } from '@xyo-network/node'

import { printError, printTitle } from '../../../../lib'

export const registerModule = (_node: DirectNodeModule) => {
  printTitle('Register Module')
  printError('TODO')
  return Promise.resolve()
}
