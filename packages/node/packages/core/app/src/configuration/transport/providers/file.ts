import { AbstractNode } from '@xyo-network/modules'

import { NodeConfigurationFunction } from '../../../model'

export const configureFileTransport: NodeConfigurationFunction = (_node: AbstractNode) => {
  // TODO: Read in file of static commands
}
